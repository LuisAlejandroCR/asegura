# .env.example — apps/web (Vite static site)
# Backend NestJS base URL — voz.html calls POST {VITE_API_URL}/voice/session to start a
# LiveKit voice session. Local dev: matches the backend's PORT (see the root .env.example).
VITE_API_URL=http://localhost:3000
